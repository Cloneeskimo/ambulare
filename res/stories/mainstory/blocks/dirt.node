block info:
{
    # specify the texture path for dirt block
    texture_paths:
    {
        resource_path: /textures/blocks/dirt/dirt.png
    }

    # specify a base path to overlay textures to use for corners/edges
    overlay_info:
    {
        resource_path: /textures/blocks/dirt/dirt
        extension: .png
    }
}
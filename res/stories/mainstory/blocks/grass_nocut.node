block info:
{
    # specify the texture path for the grass block
    texture_paths:
    {
        resource_path: /textures/blocks/grass/grass.png
    }

    # specify a cut radius of zero to avoid cutting
    cut_radius: 0
}